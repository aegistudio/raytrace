enable_testing()

macro(add_testcase_prefix prefix driver name)
	add_test(${prefix}.${name} ${driver} ${name})
endmacro()

macro(add_testcase driver name)
	add_testcase_prefix(${driver} ${driver} ${name})
endmacro()
